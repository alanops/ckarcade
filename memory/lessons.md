# Lessons

- Keyboard shortcuts must never hijack normal typing inside terminal-style inputs. In CKArcade, only non-typing assist keys such as Tab, Right Arrow, and the Enter-to-accept-suggestion path may run while the terminal input is focused; global action shortcuts like H/R/M/Space should only fire when focus is outside the terminal input.
